export default function Header() {
  return (
    <header className="h-16 w-full bg-surface border-b border-slate-200 px-6 flex items-center justify-between flex-shrink-0">
      {/* Title / Context Area Container */}
      <div className="flex items-center space-x-4" />

      {/* Global Status / Search / Profile Actions Container */}
      <div className="flex items-center space-x-4" />
    </header>
  );
}
